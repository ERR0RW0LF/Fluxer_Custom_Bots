services:
  fluxer_bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fluxer_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs

